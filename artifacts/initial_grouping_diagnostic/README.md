Initial diagnostic only, not final evaluation. A coarse perceptual rule merged similar centered-particle morphologies into one giant component. The rule was tightened using the actual images, prior to final training and any test evaluation. A concurrent discarded training run progressed during UI work; its log was read only after the image-based decision. None of its weights/results are used in the final application.
